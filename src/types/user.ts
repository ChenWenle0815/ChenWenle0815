// loginDataType 接口定义
export interface loginDataType {
    username: string;
    password: string;
  }
  
  // userInfoType 接口定义
  export interface userInfoType {
    id: number;
    name: string;
    email: string;
    age: number;
  }